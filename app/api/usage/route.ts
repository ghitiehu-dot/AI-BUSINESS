import { createClient } from "../../lib/supabase/server";
import { NextResponse } from "next/server";
export async function POST(req:Request){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});const body=await req.json().catch(()=>null);if(!body?.event_type||typeof body.event_type!=='string')return NextResponse.json({error:'event_type required'},{status:400});const units=Math.max(1,Math.min(100000,Number(body.units)||1));const {data,error}=await s.from('usage_events').insert({user_id:user.id,event_type:body.event_type,units,metadata:body.metadata??{}}).select('id,event_type,units,created_at').single();if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json(data,{status:201})}
