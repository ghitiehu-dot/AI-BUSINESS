"use client";
import { useState } from "react";
import Link from "next/link";
export default function Promos(){const [code,setCode]=useState("");const [message,setMessage]=useState("");const [busy,setBusy]=useState(false);async function redeem(e:React.FormEvent){e.preventDefault();setBusy(true);setMessage("");try{const r=await fetch('/api/promos/redeem',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({code})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Unable to redeem');setMessage(`Promo applied. ${d.credits||0} bonus credits granted.`);setCode('')}catch(err){setMessage(err instanceof Error?err.message:'Unable to redeem promo')}finally{setBusy(false)}}return <main className="page"><header className="topbar"><Link className="brand" href="/">AI BUSINESS</Link><Link href="/dashboard">Dashboard</Link></header><section className="pagehero"><div className="eyebrow">PROMO ACCESS</div><h1>Have an access code?</h1><p>Enter a code provided by the AI BUSINESS owner. Validity and redemption limits are checked securely.</p></section><section className="formpanel"><form onSubmit={redeem}><label>Promo code<input value={code} onChange={e=>setCode(e.target.value)} placeholder="ENTER-CODE" required /></label><button className="button" disabled={busy}>{busy?'Applying…':'Apply promo code'}</button></form>{message&&<div className="notice">{message}</div>}</section></main>}
