"use client";
import { useState } from "react";
import Link from "next/link";
import { createClient } from "../../lib/supabase/client";
export default function Login(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [msg,setMsg]=useState("");const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setMsg("");const {error}=await createClient().auth.signInWithPassword({email,password});if(error)setMsg(error.message);else window.location.href="/dashboard";setBusy(false)}return <main className="auth"><div className="authcard"><Link className="brand" href="/">AI BUSINESS</Link><h1>Welcome back</h1><p>Continue where you left off.</p><form onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required /></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required /></label><button className="button full" disabled={busy}>{busy?"Signing in…":"Log in"}</button></form>{msg&&<div className="notice error">{msg}</div>}<p className="switch">New here? <Link href="/create-account">Create an account</Link></p></div></main>}
