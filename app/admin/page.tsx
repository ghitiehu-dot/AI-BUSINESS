import { createClient } from "../../lib/supabase/server";
import Link from "next/link";
export default async function Admin(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return <main className="auth"><div className="authcard"><h1>Admin</h1><p>Please log in.</p><Link className="button" href="/login">Log in</Link></div></main>;const {data:profile}=await s.from('profiles').select('role').eq('id',user.id).maybeSingle();if(profile?.role!=='admin')return <main className="page"><header className="topbar"><Link className="brand" href="/">AI BUSINESS</Link><Link href="/dashboard">Dashboard</Link></header><section className="empty"><h2>Admin access required</h2><p>This area is restricted to owners/admins.</p></section></main>;return <main className="page"><header className="topbar"><Link className="brand" href="/">AI BUSINESS</Link><Link href="/dashboard">Dashboard</Link></header><section className="pagehero"><div className="eyebrow">OWNER CONTROL CENTER</div><h1>Operate AI BUSINESS.</h1><p>Monitor operational usage without default access to users' private workspace content.</p></section><section className="listgrid"><Link href="/admin/usage"><h2>Usage</h2><p>AI actions, workflow events and credit consumption.</p></Link><Link href="/admin/promotions"><h2>Promotions</h2><p>Create and manage access grants and promo codes.</p></Link><Link href="/admin/plans"><h2>Plans</h2><p>Configure pricing and monthly credit allowances.</p></Link></section></main>}
