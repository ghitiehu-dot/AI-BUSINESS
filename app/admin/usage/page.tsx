import { createClient } from "../../../lib/supabase/server";
import Link from "next/link";
export default async function AdminUsage(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return null;const {data:p}=await s.from('profiles').select('role').eq('id',user.id).maybeSingle();if(p?.role!=='admin')return <main className="page"><h1>Unauthorized</h1></main>;const {data:events}=await s.from('usage_events').select('event_type,units,created_at').order('created_at',{ascending:false}).limit(100);const total=(events??[]).reduce((n,e)=>n+e.units,0);return <main className="page"><header className="topbar"><Link className="brand" href="/">AI BUSINESS</Link><Link href="/admin">Admin</Link></header><section className="pagehero"><div className="eyebrow">OWNER / USAGE</div><h1>Usage overview.</h1><p>Operational usage metadata only. Private workspace content is not shown here.</p></section><section className="plans"><article><span>EVENTS</span><strong>{events?.length??0}</strong><p>Recent usage events</p></article><article><span>UNITS</span><strong>{total}</strong><p>Total units in recent window</p></article></section></main>}
