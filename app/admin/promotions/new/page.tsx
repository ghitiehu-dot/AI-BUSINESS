"use client";
import {useState} from "react";
import Link from "next/link";
export default function NewPromo(){const [form,setForm]=useState({code:'',credits:'100',duration_days:'30',max_redemptions:'',expires_at:''});const [msg,setMsg]=useState('');const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setMsg('');const r=await fetch('/api/admin/promotions',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...form,credits:Number(form.credits),duration_days:Number(form.duration_days),max_redemptions:form.max_redemptions?Number(form.max_redemptions):null,expires_at:form.expires_at||null})});const d=await r.json();setMsg(r.ok?`Created ${d.code}.`:d.error||'Unable to create promo');setBusy(false)}return <main className="page"><header className="topbar"><Link className="brand" href="/">AI BUSINESS</Link><Link href="/admin/promotions">Promotions</Link></header><section className="pagehero"><div className="eyebrow">OWNER / NEW PROMO</div><h1>Create controlled access.</h1><p>Generate a code with a credit grant and optional redemption/expiry limits.</p></section><form className="formpanel" onSubmit={submit}><label>Code<input required value={form.code} onChange={e=>setForm({...form,code:e.target.value.toUpperCase()})} placeholder="WELCOME100" /></label><label>Bonus credits<input type="number" min="0" value={form.credits} onChange={e=>setForm({...form,credits:e.target.value})}/></label><label>Duration (days)<input type="number" min="1" value={form.duration_days} onChange={e=>setForm({...form,duration_days:e.target.value})}/></label><label>Maximum redemptions<input type="number" min="1" value={form.max_redemptions} onChange={e=>setForm({...form,max_redemptions:e.target.value})} placeholder="Unlimited"/></label><label>Expires at<input type="datetime-local" value={form.expires_at} onChange={e=>setForm({...form,expires_at:e.target.value})}/></label><button className="button" disabled={busy}>{busy?'Creating…':'Create promo'}</button>{msg&&<div className="notice">{msg}</div>}</form></main>}
